[uwsgi]
master = True
plugins = 0:cgi

cgi = /usr/lib/cgit/cgit.cgi

# Serve static content (CSS/js) with uwsgi, so
# nginx doesn't need to know about it.
# project_dir = /usr/share/cgit/
# check-static = %(project_dir)
# static-skip-ext = .cgi

processes = 1
