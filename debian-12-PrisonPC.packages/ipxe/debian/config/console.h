// Unnecessary I guess, but might as well include them anyway as it will make debugging/etc easier in future.
#define        CONSOLE_SYSLOG          /* Syslog console */
// FIXME: Do we support syslogs? Do we set the necessary DHCP options to make it work?
#define        CONSOLE_SYSLOGS         /* Encrypted syslog console */
