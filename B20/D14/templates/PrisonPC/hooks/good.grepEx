# -*- conf -*-
# apt → sqv
sqv
# xdm → cpp
cpp
# xdm → cpp → arch-specific bullshit
cpp-x86-64-linux-gnu
# singularity → python3-numpy → python3-numpy-dev
python3-numpy-dev
# init → systemd → libsystemd-shared → libcrypt1
libcrypt1
# msmtp-mta → libsecret-1-0 → libgcrypt20
libsecret-1-0
# msmtp-mta → libsecret-1-0 → libgcrypt20
libgcrypt20
# udisks2 → libblockdev-crypto
libblockdev-crypto3
# udisks2 → libblockdev-crypto → libcryptsetup
libcryptsetup12
# libreoffice → libcurl → libngtcp2-crypto
libngtcp2-crypto-.*[0-9]
# libcups → src:krb5 stuff
libgssapi-krb5-2
# libcups → src:krb5 stuff
libk5crypto3
# libcups → src:krb5 stuff
libkrb5-3
# libcups → src:krb5 stuff
libkrb5support0
# vlc → vlc-plugins-base → libavformat64 → librist4 → libmbedcrypto16
libmbedcrypto16
